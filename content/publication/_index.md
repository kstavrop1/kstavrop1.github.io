---
title: Publications
cms_exclude: true

# View.
#   1 = List
#   2 = Compact
#   3 = Card
#   4 = Citation
view: 4

# Optional header image (relative to `static/media/` folder).
banner:
  caption: ''
  image: ''

# Don't generate a standalone page for the section list or for each paper —
# the homepage shows everything and titles link straight to arXiv.
# Pages stay in the site's page collections (list: always) so the homepage
# can still read their metadata; they just aren't rendered as HTML.
build:
  render: never
  list: always
cascade:
  build:
    render: never
    list: always
---
